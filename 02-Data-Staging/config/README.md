# Config Lookup Files — Provenance & Documentation

These JSON files are machine-readable lookup tables used by the data processing pipeline (Phase 2) to decode coded fields in the raw BTS TransBorder data. They were created on **2026-03-15** and validated/corrected on **2026-03-22**.

## How These Files Were Created

All code lookup files were **manually transcribed** from the official BTS data dictionary PDF and the Census Schedule D, then validated by cross-referencing against actual values found in the raw data. They were not auto-generated by script.

**Primary sources:**
- `01-Raw-Data/data_dictionary/codes-north-american-transborder-freight-raw-data.pdf` — Official BTS code reference (downloaded 2025-03-22 from bts.gov)
- `01-Raw-Data/data_dictionary/legacy-reference/codes_all.xls` — Legacy code tables bundled in BTS ZIPs
- Census Bureau Schedule D (port codes) — https://www.census.gov/foreign-trade/schedules/d/

**Additional sources for specific files:**
- `schema_mappings.json` — Derived from `01-Raw-Data/data_dictionary/Historical and current data format comparison.xlsx` (BTS-published) and `01-Raw-Data/data_dictionary/legacy-reference/Data Fields for all Tables.xls`
- `transborder_url_manifest.json` — Scraped from the BTS raw data download page at `bts.gov/topics/transborder-raw-data`

## Corrections Applied (2026-03-22)

After initial creation, all files were validated against the official BTS codes PDF. The following corrections were applied:

| File | Issue | Fix |
|---|---|---|
| `canadian_province_codes.json` | Multiple entries were completely wrong (incorrect code-to-name mappings) | Rebuilt from scratch using the BTS PDF |
| `mexican_state_codes.json` | Code `BN` listed instead of `BC` for Baja California; `OT` (State Unknown) was missing | Changed `BN` → `BC`; added `OT` |
| `mode_codes.json` | Minor label wording differences from official PDF | Updated labels to match PDF exactly |

## File Reference

### Code Lookup Tables

These decode the numeric/alpha codes in the raw BTS data into human-readable values. Each maps a raw code (key) to a description (value).

| File | Raw Field | Entries | Format | Notes |
|---|---|---|---|---|
| `mode_codes.json` | `DISAGMOT` | 9 | `{"code": "name"}` | 1=Vessel, 3=Air, 4=Mail, 5=Truck, 6=Rail, 7=Pipeline, 8=Other, 9=FTZ. No code 2. |
| `commodity_codes.json` | `COMMODITY2` | 99 | `{"code": "description"}` | HS (Harmonized Schedule) 2-digit codes (01–99). NOT SCTG. |
| `country_codes.json` | `COUNTRY` | 2 | `{"code": "name"}` | 1220=Canada, 2010=Mexico. |
| `trade_type_codes.json` | `TRDTYPE` | 2 | `{"code": "name"}` | 1=Export, 2=Import. |
| `state_codes.json` | `USASTATE` | 57 | `{"code": "name"}` | 50 states + DC + 6 territories. Standard USPS 2-letter codes. |
| `canadian_province_codes.json` | `CANPROV` | 15 | `{"code": "name"}` | X-prefix codes (XA=Alberta, XC=British Columbia, etc.) + OT=Unknown. |
| `mexican_state_codes.json` | `MEXSTATE` | 32 | `{"code": "name"}` | 2-letter codes for 31 states + OT=Unknown. Note: raw data from Apr 1994–May 1998 uses erroneous code `BN` for Baja California — `03_normalize.py` must remap BN→BC. |
| `schedule_d_port_codes.json` | `DEPE` | ~300 | `{"code": {"port", "district", "state"}}` | 4-digit Census Schedule D port codes. Each entry includes port name, customs district, and state. |

### Schema & Mapping Files

| File | Purpose | Notes |
|---|---|---|
| `schema_mappings.json` | Maps legacy (1993–2006) column names and table structures to the modern (2007+) format | Contains: field_mappings, legacy_table_definitions, date_mappings, processing_caveats, known_issues. Heavily commented with `_note` fields. |
| `port_aliases.json` | Maps variant port name spellings to canonical names | **Currently empty** — populated iteratively during Phase 2 normalization as spelling variants are discovered in the data. |
| `transborder_url_manifest.json` | Inventory of all BTS download URLs | 14 legacy + 57 modern ZIP URLs. Used for download verification, not by the processing pipeline. Source field references `transborder_raw_data_links.md`. |

## Known Data Quality Issues

These are documented in `schema_mappings.json` under `processing_caveats` and `known_issues`:

1. **MEXSTATE BN→BC**: Raw data from Apr 1994–May 1998 uses code `BN` (Baja California Norte) instead of the correct `BC`. The normalization script must remap this.
2. **2006 DBF field names have trailing spaces**: e.g., `'disagmot  '` — must `.strip()` field names when reading.
3. **STATMOYR format changed**: 4-char `MMYY` (1993–1997) → 6-char `YYYYMM` (1998–2006). Date parsing must handle both.
4. **2006 DOT-prefixed files** use a wider schema (254-char fields, separate MONTH/YEAR columns) that bridges to the post-2007 format.
